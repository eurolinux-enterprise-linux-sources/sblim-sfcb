diff -up sblim-sfcb-1.3.11/sfcb.init-redhat.in.orig sblim-sfcb-1.3.11/sfcb.init-redhat.in
--- sblim-sfcb-1.3.11/sfcb.init-redhat.in.orig	2016-10-03 13:45:04.000000000 +0200
+++ sblim-sfcb-1.3.11/sfcb.init-redhat.in	2016-10-03 13:44:16.000000000 +0200
@@ -32,6 +32,7 @@ start() {
 	echo -n "Starting sfcb: "
 	daemon $SFCB_BIN -d &&
 	touch $LOCKFILE &&
+	echo &&
 	return 0 ||
 	return 1
 }	
@@ -44,12 +45,14 @@ stop() {
 	echo -n "Shutting down sfcb: "
 	killproc sfcbd &&
 	rm -f $LOCKFILE &&
+	echo &&
 	return 0 || return 1
 }
 
 reload() {
 	echo -n "Reloading sfcb configuration: "
 	killproc sfcbd -HUP &&
+	echo &&
 	return 0 || return 1
 }
 
@@ -64,10 +67,10 @@ case "$1" in
 	pid=`pidofproc $SFCB_BIN`
 	RETVAL=$?
 	if [ "$RETVAL" -eq 0 ]; then
-		echo -n $"sfcb ($pid) is running"
+		echo $"sfcb ($pid) is running"
 		exit 0
 	else
-		if [ -e /var/run/tog-pegasus.pid ]; then
+		if [ -e /var/run/sblim-sfcb.pid ]; then
 			echo $"sfcb is not running, but pid file exists"
 			exit 1
 		elif [ -e $LOCKFILE ]; then
